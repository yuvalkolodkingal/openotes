build
coverage
node_modules
dist
.DS_STORE
nx-cloud.env
.idea
.env.local
.nyc_output
site
node_modules.backup
.nx
/*.patch
.taskcache
# Deno desktop build artifacts
apps/desktop/native/
apps/desktop/.native-build/
apps/desktop/dist/
# The release tarball CI drops beside the flatpak manifest before building
packaging/flatpak/*.tar.gz
apps/desktop/.build/
apps/desktop/ui/
dist/

# Vite writes a timestamped copy of a .mts config next to it while loading.
*.timestamp-*.mjs

# TypeScript's incremental build cache. Machine-specific, rewritten on
# every build, and 436 KB of it was committed in 2.1 — enough to bury a
# real diff.
*.tsbuildinfo
