build
coverage
node_modules
dist
.DS_STORE
nx-cloud.env
.idea
.env.local
.nyc_output
site
node_modules.backup
.nx
/*.patch
.taskcache
# Deno desktop build artifacts
apps/desktop/native/
apps/desktop/.native-build/
apps/desktop/dist/
# The release tarball CI drops beside the flatpak manifest before building
packaging/flatpak/*.tar.gz
apps/desktop/.build/
apps/desktop/ui/
dist/

# deno task dev builds here so a development build never looks like a release
.dev/
